name: Rust
images:
  - !!var images.rust_lang

install:
  script: !!file install.sh
  container: "debian:buster-slim"
  entrypoint: "bash"

config:
  stop: "^C"
  startup:
    done: ""

startup: bash ./start.sh
